import { Lilita_One, Rubik } from 'next/font/google';

export const lilita_one = Lilita_One(
    {
        subsets: ['latin'],
        weight: ['400'],
    }
);

export const rubik = Rubik(
    {
        subsets: ['latin'],
        weight: ['300', '400', '500', '600', '700'],
    }
);